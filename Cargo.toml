[package]
name = "gipbridge"
version = "0.1.0"
edition = "2021"

[dependencies]
rusb = "0.9"
anyhow = "1"
hex = "0.4"
libc = "0.2"
clap = { version = "4", features = ["derive"] }

[profile.release]
lto = true
codegen-units = 1
